export default function league() {
    // MOCK-UP for Handler/Select
    // Remove during merge!
    return new Promise((resolve, reject) => {
        setTimeout(() => resolve(["League 1A", "League 2B", "League 3C"]), 700);
    })
}
