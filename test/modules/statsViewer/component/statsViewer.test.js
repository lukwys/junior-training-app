import React from 'react';
import ReactDOM from 'react-dom';
import { observer } from 'mobx-react';

import { StatsViewerComponent } from '../../../../src/modules/statsViewer/component/statsViewer'

describe('modules/statsViewer / components/statsViewer', () => {
    describe('when true', () => {
        it('then true is true', () => {
            expect(true).toBe(true)
        });
    })
});
