import React from 'react';
import ReactDOM from 'react-dom';
import { observer } from 'mobx-react';

import { SourceComponent } from '../../../../../../src/modules/statsViewer/modules/source/components/source'

describe('modules/statsViewer / modules/source / components/source', () => {
    describe('when true', () => {
        it('then true is true', () => {
            expect(true).toBe(true)
        });
    })
});
