import React from 'react';
import ReactDOM from 'react-dom';
import { observer } from 'mobx-react';

import { InputDataListComponent } from '../../../../../../src/modules/statsViewer/modules/selects/components/inputDataList'

describe('modules/statsViewer / modules/selects / components/inputDataList', () => {
    describe('when true', () => {
        it('then true is true', () => {
            expect(true).toBe(true)
        });
    })
});
