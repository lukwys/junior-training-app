import React from 'react';
import ReactDOM from 'react-dom';
import { observer } from 'mobx-react';

import { InputComponent } from '../../../../../../src/modules/statsViewer/modules/selects/components/input'

describe('modules/statsViewer / modules/selects / components/input', () => {
    describe('when true', () => {
        it('then true is true', () => {
            expect(true).toBe(true)
        });
    })
});
