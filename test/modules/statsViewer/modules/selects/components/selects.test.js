import React from 'react';
import ReactDOM from 'react-dom';
import { observer } from 'mobx-react';

import { SelectsComponent } from '../../../../../../src/modules/statsViewer/modules/selects/components/selects'

describe('modules/statsViewer / modules/selects / components/selects', () => {
    describe('when true', () => {
        it('then true is true', () => {
            expect(true).toBe(true)
        });
    })
});
