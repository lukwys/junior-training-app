import React from 'react';
import ReactDOM from 'react-dom';
import { observer } from 'mobx-react';

import { ResultsComponent } from '../../../../../../src/modules/statsViewer/modules/results/components/results'

describe('modules/statsViewer / modules/results / components/results', () => {
    describe('when true', () => {
        it('then true is true', () => {
            expect(true).toBe(true)
        });
    })
});
